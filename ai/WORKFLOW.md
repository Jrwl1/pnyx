WORKFLOW.md — Default Operating Loop

Read AGENTS.md → LOADOUT → RULES → PROJECT_STATUS.

Route-select at most 2 additional docs.

Plan (max 7 bullets). If plan grows, split into tasks.

Implement in small batches.

Prove: run relevant checks (lint/typecheck/tests).

Log: append WORKLOG entry with proof + commit hash.

Update status docs only when appropriate (Coordinator owns final status closeout).

Modes:

Planning Mode: ai/workflows/PLANNING_MODE.md

Roadmap Mode: ai/workflows/ROADMAP_MODE.md

DO Mode: ai/workflows/DO_MODE.md
